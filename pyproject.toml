[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
[project]
name = "payloadlab"
version = "0.1.0"
description = "Safe metadata catalog for defensive testing material"
requires-python = ">=3.9"
[tool.pytest.ini_options]
pythonpath = ["src"]
